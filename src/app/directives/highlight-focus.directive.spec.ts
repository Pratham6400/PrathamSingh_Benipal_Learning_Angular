import { HighlightFocusDirective } from './highlight-focus.directive';

describe('HighlightFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightFocusDirective();
    expect(directive).toBeTruthy();
  });
});
